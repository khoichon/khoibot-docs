name: Publish docs to Github Pages with Jekyll

on:
  push:
    branches: ["main"] # Change to your default branch

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2' # Standard stable Ruby version
          bundler-cache: true # Caches dependencies for fast builds

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build with Jekyll
        # This builds your Markdown files into HTML using the Lime theme configurations
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

